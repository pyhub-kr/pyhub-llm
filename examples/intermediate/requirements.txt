# 중급 예제 실행에 필요한 패키지
pyhub-llm

# 선택적 패키지 (특정 예제에만 필요)
# sqlalchemy  # 06_history_backup.py의 SQLAlchemyHistoryBackup 사용 시
# aiofiles    # 비동기 파일 처리 시

# 환경 변수 필요:
# export OPENAI_API_KEY="your-api-key"

# 일부 예제에서 사용하는 추가 패키지
# - scikit-learn: 임베딩 유사도 계산 (설치: pip install scikit-learn)
# - numpy: 벡터 연산 (설치: pip install numpy)